{% extends 'base.html.twig' %}

{% block title %}Your one stop place for wizarding supplies{% endblock %}

{% block body %}
  <div class="row">
    <div class="col-md-12">
      <div class="d-flex align-content-stretch flex-wrap">
        {% for item in items %}
          {{ include('item/item.html.twig', {'item': item}) }}
        {% endfor %}
      </div>
    </div>
  </div>
{% endblock %}
